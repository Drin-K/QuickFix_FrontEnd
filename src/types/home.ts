export type ServiceCategory = {
  title: string;
  description: string;
  icon: string;
};

export type Statistic = {
  label: string;
  value: string;
};

export type ProviderHighlight = {
  name: string;
  specialty: string;
  rating: number;
  city: string;
};
